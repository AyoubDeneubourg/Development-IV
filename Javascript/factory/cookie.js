const cookie = function ({ name, price, costPrice, size, weight }) {
  this.name = name;
  this.price = price;
  this.costPrice = costPrice;
  this.size = size;
  this.weight = weight;
};

export default cookie;

